# ============================================
# Environment variables para Docker Compose
# ============================================
# Copiar a .env para usar en desarrollo local

# Database Configuration
DB_ROOT_PASSWORD=
DB_USER=noteforge_user
DB_PASSWORD=
DB_NAME=note_forge

# API Configuration
JWT_SECRET=
NODE_ENV=development

# URLs (dentro de Docker network)
BASE_URL=http://api:5000
FRONTEND_URL=http://localhost:5173
